--- a/src/SFML/Graphics/CMakeLists.txt
+++ b/src/SFML/Graphics/CMakeLists.txt
@@ -109,8 +109,30 @@
 endif()
 
 if(SFML_USE_SYSTEM_DEPS)
-    find_package(Freetype REQUIRED)
-    find_package(HarfBuzz REQUIRED)
+    if(NOT TARGET Freetype::Freetype AND TARGET deps::freetype)
+        set(_CATALOG_FREETYPE_TARGET deps::freetype)
+        get_target_property(_CATALOG_FREETYPE_REAL ${_CATALOG_FREETYPE_TARGET} ALIASED_TARGET)
+        while(_CATALOG_FREETYPE_REAL)
+            set(_CATALOG_FREETYPE_TARGET ${_CATALOG_FREETYPE_REAL})
+            get_target_property(_CATALOG_FREETYPE_REAL ${_CATALOG_FREETYPE_TARGET} ALIASED_TARGET)
+        endwhile()
+        add_library(Freetype::Freetype ALIAS ${_CATALOG_FREETYPE_TARGET})
+    endif()
+    if(NOT TARGET Freetype::Freetype)
+        find_package(Freetype REQUIRED)
+    endif()
+    if(NOT TARGET HarfBuzz::HarfBuzz AND TARGET deps::harfbuzz)
+        set(_CATALOG_HARFBUZZ_TARGET deps::harfbuzz)
+        get_target_property(_CATALOG_HARFBUZZ_REAL ${_CATALOG_HARFBUZZ_TARGET} ALIASED_TARGET)
+        while(_CATALOG_HARFBUZZ_REAL)
+            set(_CATALOG_HARFBUZZ_TARGET ${_CATALOG_HARFBUZZ_REAL})
+            get_target_property(_CATALOG_HARFBUZZ_REAL ${_CATALOG_HARFBUZZ_TARGET} ALIASED_TARGET)
+        endwhile()
+        add_library(HarfBuzz::HarfBuzz ALIAS ${_CATALOG_HARFBUZZ_TARGET})
+    endif()
+    if(NOT TARGET HarfBuzz::HarfBuzz)
+        find_package(HarfBuzz REQUIRED)
+    endif()
 else()
     # use a block to scope option variables we have to set
     block()
--- a/src/SFML/Audio/CMakeLists.txt
+++ b/src/SFML/Audio/CMakeLists.txt
@@ -69,8 +69,38 @@
 
 # find external libraries
 if(SFML_USE_SYSTEM_DEPS)
-    find_package(Vorbis REQUIRED)
-    find_package(FLAC REQUIRED)
+    if(NOT TARGET Vorbis::vorbis AND TARGET deps::libvorbis)
+        set(_CATALOG_VORBIS_TARGET deps::libvorbis)
+        get_target_property(_CATALOG_VORBIS_REAL ${_CATALOG_VORBIS_TARGET} ALIASED_TARGET)
+        while(_CATALOG_VORBIS_REAL)
+            set(_CATALOG_VORBIS_TARGET ${_CATALOG_VORBIS_REAL})
+            get_target_property(_CATALOG_VORBIS_REAL ${_CATALOG_VORBIS_TARGET} ALIASED_TARGET)
+        endwhile()
+        add_library(Vorbis::vorbis INTERFACE IMPORTED)
+        target_link_libraries(Vorbis::vorbis INTERFACE ${_CATALOG_VORBIS_TARGET})
+        if(NOT TARGET Vorbis::vorbisenc)
+            add_library(Vorbis::vorbisenc ALIAS Vorbis::vorbis)
+        endif()
+        if(NOT TARGET Vorbis::vorbisfile)
+            add_library(Vorbis::vorbisfile ALIAS Vorbis::vorbis)
+        endif()
+    endif()
+    if(NOT TARGET Vorbis::vorbis)
+        find_package(Vorbis REQUIRED)
+    endif()
+    if(NOT TARGET FLAC::FLAC AND TARGET deps::libflac)
+        set(_CATALOG_FLAC_TARGET deps::libflac)
+        get_target_property(_CATALOG_FLAC_REAL ${_CATALOG_FLAC_TARGET} ALIASED_TARGET)
+        while(_CATALOG_FLAC_REAL)
+            set(_CATALOG_FLAC_TARGET ${_CATALOG_FLAC_REAL})
+            get_target_property(_CATALOG_FLAC_REAL ${_CATALOG_FLAC_TARGET} ALIASED_TARGET)
+        endwhile()
+        add_library(FLAC::FLAC INTERFACE IMPORTED)
+        target_link_libraries(FLAC::FLAC INTERFACE ${_CATALOG_FLAC_TARGET})
+    endif()
+    if(NOT TARGET FLAC::FLAC)
+        find_package(FLAC REQUIRED)
+    endif()
 else()
     # use an immediately invoked function to scope option variables we have to set
     block()
